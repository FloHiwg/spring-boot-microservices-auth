spring:
  application:
    name: auth-service
  cloud:
    config:
      uri: http://localhost:8888
      fail-fast: true
      password: 1234
      username: user

debug: true
trace: true
logging:
  level:
    org:
      springframework:
        cloud:
          netflix: trace